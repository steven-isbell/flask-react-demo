{
  "name": "filler",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start-dev": "concurrently --kill-other \"react-scripts start\" \"FLASK_ENV=development python server/server.py\"",
    "start-prod": "FLASK_ENV=production python server/server.py",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:5000",
  "devDependencies": {
    "concurrently": "^3.6.1"
  }
}
